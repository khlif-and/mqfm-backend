version: "2"
sql:
  - engine: "mysql"
    queries: "query/"
    schema: "migration/"
    gen:
      go:
        package: "sqlcdb"
        out: "../../internal/adapter/repository/sqlcdb"
        emit_json_tags: true
        emit_prepared_queries: true
        emit_interface: true
